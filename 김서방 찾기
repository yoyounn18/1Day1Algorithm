function solution(seoul) {
    let answer;
    const targetString = 'Kim';
    
    for(let i = 0; i < seoul.length; i ++) {
        if(seoul[i] === targetString) {
            answer = i;
        }
    }
    return `김서방은 ${answer}에 있다`
}
